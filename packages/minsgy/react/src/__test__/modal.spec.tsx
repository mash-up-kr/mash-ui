describe("<Modal />", () => {
  describe("Initialize", () => {
    it("Modal.Root에 필수 prop을 전달하지 않으면 에러가 발생한다", () => {
      // given
      // when
      // then
    })
    it("Modal.Root isOpen를 true로 초기화하면 모달이 열린다", () => {
      // given
      // when
      // then
    })
  })

  describe("Render", () => {
    it("Modal.Root를 감싸지 않고", () => {
      it("Modal.Content, Modal.Header, Modal.Footer, Modal.Trigger를 사용하면 에러가 발생한다", () => {
        // given
        // when
        // then
      })
    })
    it("Modal.Root를 감싸고", () => {
      it("Modal.Content, Modal.Header, Modal.Footer, Modal.Trigger를 사용하면 렌더링된다", () => {
        // given
        // when
        // then
      })
    })
  })

  describe("Event", () => {
    it("Modal.Trigger 하위 컴포넌트를 클릭하면 모달이 열린다", () => {
      // given
      // when
      // then
    })
  })
})
